# create a code challenge solution that gets the maximum value of a key from a dictionary
def max_key_dictionary(dictionary):
    return max(dictionary.values())

print(max_key_dictionary({'a':1, 'b':2, 'c':3}))
